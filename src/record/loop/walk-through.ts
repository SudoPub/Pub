/**
 * @author WMXPY
 * @namespace Record_Loop
 * @description Walk Through
 */

import { PubRecord } from "../record";

export const walkThrough = (
    record: PubRecord,
): PubRecord => {

    return record;
};
